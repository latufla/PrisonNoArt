namespace Honeylab.Utils.Analytics
{
    public static partial class CommonAnalytics
    {
        public static class PrefsKeys
        {
            public const string SecondsPassedGlobal = nameof(SecondsPassedGlobal);
            public const string AppInstallTimestamp = nameof(AppInstallTimestamp);
            public const string SessionCount = nameof(SessionCount);
            public const string InterstitialsWatchCount = nameof(InterstitialsWatchCount);
            public const string RewardedVideosWatchCount = nameof(RewardedVideosWatchCount);
        }
    }
}
